all:
  children:
    etcd_cluster:
      hosts:
        etcd1:
          ansible_host : "{{ etcd1_and_haproxy }}"
        etcd2:
          ansible_host: "{{ etcd2 }}"
        etcd3:
          ansible_host: "{{ etcd3 }}"
    postgres_cluster:
      hosts:
        postges1:
          ansible_host: "{{ postgres1 }}"
        postgres2:
          ansible_host: "{{ postgres2 }}"
